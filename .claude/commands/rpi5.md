---
description: Debug or deploy the iot-fetcher stack on rpi5 via SSH
allowed-tools: Bash, Read, Grep, Glob
---

## Context
- SSH host: `rpi5` (IP: 192.168.68.87)
- Working dir on rpi5: `~/iot-fetcher`
- All docker commands require `sudo`
- Always use both compose files: `sudo docker compose -f docker-compose.yml -f docker-compose.local.yml`
- env_file references are only in `docker-compose.local.yml`

## Your Task

Based on user input, either **debug** or **deploy** the stack on rpi5.

### Debug Workflow

1. SSH into rpi5 and check container status: `ssh rpi5 "cd ~/iot-fetcher && sudo docker compose -f docker-compose.yml -f docker-compose.local.yml ps"`
2. Check logs for failing containers: `ssh rpi5 "sudo docker logs --tail 50 <container>"`
3. If needed, restart specific services: `ssh rpi5 "sudo docker restart <container>"`
4. Validate services are healthy after restart

### Deploy Workflow

1. SSH into rpi5 and pull latest code: `ssh rpi5 "cd ~/iot-fetcher && git pull"`
2. Pull latest images: `ssh rpi5 "cd ~/iot-fetcher && sudo docker compose -f docker-compose.yml -f docker-compose.local.yml pull"`
3. Restart stack: `ssh rpi5 "cd ~/iot-fetcher && sudo docker compose -f docker-compose.yml -f docker-compose.local.yml up -d"`
4. Check that all containers are running

### Guardrails
- Never run docker commands without `sudo`
- Never use only one compose file — always include both `-f docker-compose.yml -f docker-compose.local.yml`
- Do not modify env files on rpi5 without asking first

---
*Generated by /reflect-skills from 5+ session patterns*
