# Where the webui proxy reaches the ai-brain introspection API (GET only).
# Optional: the default already resolves on the compose network, so this only
# needs setting for local development outside Docker (e.g. http://localhost:8091).
# Set it to an empty string to disable the /ai-brain page's backend (503).
AI_BRAIN_URL=http://ai-brain:8091
