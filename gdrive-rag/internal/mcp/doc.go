// Package mcp exposes the Model Context Protocol server surface so Claude and
// other MCP clients can query the index. See server.go for NewServer, the
// entry point main.go composes against.
package mcp
